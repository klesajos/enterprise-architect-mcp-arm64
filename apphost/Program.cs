// Placeholder. This code is never used or shipped - the project exists only so the
// .NET SDK emits a native arm64 apphost (MCP3.exe). See MCP3.csproj for details.
class Program { static void Main() { } }
